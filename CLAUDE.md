# ПУ-5 — төмөр замын дэр/дүнз шалгалтын апп

Монголын төмөр замын замын ангиуд (ПД-1 … ПД-13) дэр, сумын дүнзний
үзлэгээ утсаараа бүртгэж, ПУ-5 дэвтэр болон Excel тайлан гаргадаг.
Талбар дээр, ихэвчлэн 4G-ээр ажилладаг тул **офлайн ажиллагаа,
мобайл дата, аппын найдвартай байдал** нь чухал.

## Хэлний дүрэм

Хэрэглэгчид харагдах бүх зүйл, commit, PR **монголоор**. Кодын
тайлбар мөн монголоор — яагаад ийм болсныг тайлбарла, юу хийж
байгааг биш.

## Бүтэц

Апп бол **ганц файл**: `index.html` (~8900 мөр).

- `<style>` — CSS токен, `:root` ба `[data-theme="dark"]`
- Хоёр inline `<script>`:
  1. **Алдаа харуулагч** — бүх скриптээс өмнө. JS унавал хар дэлгэцийн
     оронд шалтгааныг улаанаар бичнэ. Битгий доош зөө.
  2. Аппын бүх логик
- Гадны сан: Firebase 9.23 (gstatic), ExcelJS 4.3 (cdnjs)

Дэлгэцүүд: `loginView`, `homeView` (дэр), `swHomeView` (сум/СШ),
`trackView`, `recordView`, `swRecView`, `mainKmView`, `summaryView`,
`adminView`.

## Өгөгдөл

Хэсэг бүрийн БҮХ өгөгдөл **ганц Firestore баримтад**: `sections/<код>`
(жишээ `sections/ПД-6`). Утсанд `localStorage['sg_der_v3_<код>']`.

- `DB.folders[]` — дэрийн паспорт, дотор нь `tracks[].sections[].sleepers[]`
- `DB.main[]` — **гол зам, паспортаас ГАДУУР** (давтагддаггүй)
- `DB.sw[]` — сумын паспорт, дотор нь `turnouts[]`
- `_packDB` дэр бүрийг **1 тэмдэгт** болгож шахдаг (`'nnbn…'`)

**Солилтын бүртгэл** (`sec.repl`, `turnout.dRepl`) нь шинэ паспортад
ХУУЛАГДДАГГҮЙ — `resolveReplMap` / `swResolveDRepl` нь өмнөх
паспортууд руу ухарч хайдаг. Тиймээс **хуучин паспорт уствал
солигдсон огноо алга болно**.

## Хатуу дүрмүүд

1. **Хувилбарыг ХОЁР газар**: `index.html` дэх `APP_BUILD` ба
   `version.txt`. Зөрвөл CI унана. `·` ба `-` ялгаатай:
   `APP_BUILD='2026.08.28·99'` ↔ `version.txt` = `2026.08.28-99`
2. **Нэгтгэлийн дараа ЗААВАЛ дахин шалга.** v87-д зөрчлийн тэмдэглэгээ
   үлдэж апп бүхэлдээ унасан. `/release` skill-ийг дага.
3. **`git checkout --ours` хийхээс өмнө** `git diff HEAD origin/main`
   харж, main дээр өвөрмөц зүйл байхгүйг батал. Байвал гараар шийд.
4. **Firestore-ын баримтын нэр** `__…__` хэлбэртэй байж БОЛОХГҮЙ
   (нөөцлөгдсөн). `tests/docid.js` шалгана.
5. **Тест орчинд Firebase хуурамч** (`tests/stub.js`). Үүлтэй холбоотой
   өөрчлөлтийг тестээр батлах БОЛОМЖГҮЙ — бодит нөхцөлд тусад нь турш.
6. **Эрсдэлтэй шинэ зүйлийг шууд бүү асаа.** v94-д хос бичилтийг
   шалгалгүй асааж асуудал үүсгэсэн. Загвар: урьдчилсан туршилт →
   хэрэглэгч өөрөө асаах → 3 удаа алдвал өөрөө унтрах.

## Шалгалт

```bash
cd tests
node allA.js && node allB.js      # 206 шалгалт
node sweepA.js && node sweepB.js  # 7 утасны байрлал
```

Дэлгэрэнгүйг `tests/README.md`-ээс.

## Явцын ажил

**Паспорт бүрийг тусдаа Firestore баримт болгох** (1 МиБ хязгаарыг
арилгах, мобайл датаг ~14 дахин бууруулах, зэрэг засварын мөргөлдөөнийг
зогсоох):

- **Шат 1 — хос бичилт ✓ дууссан.** `sections/<код>/parts/*` руу зэрэг
  бичнэ, унших нь хуучин баримтаас. Мэдэгдэл дэлгэцээс асаана
  (`sg_parts_on`). ПД-6 дээр батлагдсан.
- **Шат 2 — унших талыг шилжүүлэх.** Хийгдээгүй.
- **Шат 3 — хос бичилтийг зогсоох, хуучныг архивлах.** Хийгдээгүй.

Firestore-ын дүрэмд `match /parts/{partId}` нэмэгдсэн, хэсэг тус бүр
тусгаарлагдсан (ПД-6 → `pd-6@railway.mn`).
